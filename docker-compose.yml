version: '3.8'

services:
  bot:
    build: .
    container_name: learnjp_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./credentials:/app/credentials
    environment:
      - PYTHONUNBUFFERED=1
